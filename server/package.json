{
  "name": "analytic-server",
  "version": "0.0.1",
  "description": "Analytic server to send kiosk dashboard data to MangoDB",
  "main": "server.js",
  "scripts": {
    "test": "node --trace-warnings server.js",
    "start": "node --watch --trace-warnings server.js"
  },
  "keywords": [
    "kiosk",
    "dashboard",
    "analytic",
    "server"
  ],
  "author": "bhaumic",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "mongoose": "^8.2.0"
  }
}
